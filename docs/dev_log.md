# Registro de desarrollo

## 2024-08-19
- Actualicé `lib/ui/auth/register_page.dart` para añadir la opción **Continuar con Google**, manejar su estado de carga y retirar el interruptor de "Solicitar verificación administrativa".
- El botón secundario de Google aprovecha el nuevo soporte de íconos en `AppSecondaryButton` y deja preparado el flujo de OAuth con Supabase. Se muestra un aviso si la sesión no llega inmediatamente, indicando que el usuario debe finalizar el proceso en el navegador.

## 2025-10-20
- Reconstruí la hoja de compartir creando `share_sheet.dart`, con encabezado, opciones consistentes con la paleta y acciones reutilizables para copiar enlace o abrir redes sociales desde el detalle de campaña.
- Simplifiqué la navegación de `HomePage`: el menú lateral incluye "Cerrar sesión" y la barra inferior solo muestra Campañas y Organizaciones, evitando duplicar accesos con el drawer.
- Añadí políticas RLS para `public.profiles`, permitiendo que cada usuario lea/actualice su propio registro y que administradores accedan a todos los perfiles incluso con seguridad activada.
- Corregí la navegación en `lib/ui/auth/register_page.dart`: el botón de retroceso ahora fuerza `Navigator.maybePop` para salir del registro y el botón "Iniciar sesión" abre `LoginPage`, propagando `AuthFlowResult.signedIn` cuando el usuario completa el inicio de sesión.
- Actualicé `public.handle_new_auth_user` en `supabase/supabase.sql` para priorizar `display_name` (o alternativas como `full_name`) y evitar que se rellene con el correo; añadí una actualización masiva para perfiles existentes y ajusté `ProfileService.ensureCurrentProfile()` para respetar los mismos criterios en el cliente.
- Reemplacé la pantalla de éxito del registro por un diálogo flotante en `RegisterPage` que permite saltar directo a iniciar sesión o cerrar, devolviendo el `AuthFlowResult` correspondiente.
- Rediseñé `PendingVerificationView` como tarjeta flotante con feedback y el botón "Ya confirmé mi correo" ahora intenta refrescar la sesión, muestra mensajes claros y sugiere iniciar sesión cuando aún no se detecta la verificación.
- `LoginPage` ahora acepta un correo inicial y mensajes informativos, y `RegisterPage` redirige automáticamente al login tras crear la cuenta con el email prellenado y una nota para verificar el correo.
- Habilité la creación de solicitudes de campaña con `CreateSolicitudPage`, respaldada por `SolicitudController` y `SolicitudService`, agregando validaciones, selección de categoría y feedback al enviar.
- Modelé `Solicitud`, `SolicitudDraft`, `SolicitudCategory` y `SolicitudOrganization` en `lib/models/solicitud.dart` para mantener los datos tipados en el cliente.
- Incorporé políticas RLS y el trigger `notify_admins_new_solicitud` en `supabase/supabase.sql`, notificando automáticamente a los administradores cuando se registra una nueva solicitud y permitiendo que cada usuario gestione las propias.
- Actualicé `HomePage` para abrir la pantalla de solicitud desde el CTA inicial y refrescar la lista tras enviar una solicitud exitosa.
- Reemplacé la antigua pantalla de `ProfileCompletionPage` por `ProfileSettingsPage` (`lib/ui/profile/profile_settings_page.dart`), que ahora sube la foto y el QR de donaciones directamente a Supabase Storage usando `image_picker` y valida el resto de campos sin pedir enlaces manuales.
- Añadí `ProfileOverviewPage` (`lib/ui/profile/profile_overview_page.dart`) para que los usuarios consulten sus datos verificados en una vista separada con indicadores de estado y acceso rápido a la edición.
- Actualicé `HomePage` para abrir la vista de perfil y reutilizar `ProfileSettingsPage` cuando se requiere completar datos antes de enviar una solicitud.
- Extendí `ProfileService` con métodos de subida (`uploadAvatar`, `uploadDonationQr`) que generan URLs públicas controladas en el bucket `perfiles`.
- Registré la dependencia `image_picker` en `pubspec.yaml` y ejecuté `flutter pub get` para dejarla disponible en el proyecto.
- Añadí los mensajes de privacidad requeridos en `ios/Runner/Info.plist` para solicitar acceso a la galería al cargar fotos desde dispositivos Apple.
- Ajusté `ProfileOverviewPage` para que el encabezado refluya en pantallas angostas sin overflows y declaré los permisos de lectura de imágenes en `android/app/src/main/AndroidManifest.xml` para que el selector de fotos funcione en Android 13+.
- Simplifiqué `ProfileSettingsPage` para que el usuario solo elija su departamento y escriba la dirección; el perfil ahora guarda el departamento directamente y el botón **Usar mi ubicación** maneja permisos con timeout y mensajes claros.
- Declaré permisos de ubicación en `android/app/src/main/AndroidManifest.xml` y añadí las descripciones de uso correspondientes en `ios/Runner/Info.plist` para que la solicitud de ubicación funcione en ambas plataformas.
- Documenté `ndkVersion = "27.0.12077973"` en `android/app/build.gradle.kts` para alinear el proyecto con los plugins que exigen esa versión y evitar el aviso en `flutter run`.
- Refiné `ProfileOverviewPage` reduciendo tipografías en las tarjetas y ajustando el encabezado para que el chip de estado nunca desborde en pantallas angostas.
- Añadí un botón Ver/Ocultar sobre el QR de donación en `ProfileOverviewPage`, con animación al mostrar/ocultar para proteger la información cuando no se desea exponerla en pantalla.
- Hice `ProfileSettingsPage` más responsiva: la tarjeta introductoria y la sección de foto ahora reorganizan su contenido en columnas cuando el ancho es reducido, evitando los avisos de overflow en dispositivos angostos.
- Establecí `isExpanded: true` en los dropdowns de `ProfileSettingsPage` para que ocupen todo el ancho disponible y no generen overflows en móviles.
- Ajusté `ProfileOverviewPage` para mostrar/ocultar el número de cuenta con un botón de ojo y amplié el umbral responsivo del encabezado para evitar el mensaje de overflow.
- Reorganicé el encabezado de `ProfileOverviewPage` con `LayoutBuilder` para que el avatar y el chip se apilen en pantallas angostas, eliminando el aviso “right overflowed”.
- Compacté el chip de “Perfil verificado” reduciendo paddings, icono y tipografía para que encaje mejor junto al nombre incluso en dispositivos pequeños.

## 2025-10-20
## 2025-10-21
- Reconstruí la hoja de compartir separando `share_sheet.dart`, con encabezado y opciones consistentes con la paleta institucional para copiar el enlace o abrir redes sociales desde el detalle de campaña.
- Permití iniciar `CreateSolicitudPage` directamente en la selección de tipo cuando se abre desde el botón flotante de la pantalla principal.
- Revisé `CreateSolicitudPage` para equilibrar la tipografía del formulario de campañas, añadir ejemplos más claros en los campos, limitar la descripción a 1200 caracteres y enriquecer la captura del monto con prefijo en bolivianos.
- Eliminé los campos adicionales del modo campaña y sumé una galería de evidencias con soporte para múltiples imágenes, reutilizando `SolicitudController`/`SolicitudService` para subirlas y anexarlas al borrador.
- Reintroduje en `CreateSolicitudPage` los datos del beneficiario con validaciones dedicadas (nombre obligatorio y relación seleccionable) y elevé el tope de evidencias cargables mostrando el contador disponible.
- Ajusté las subidas de portada y evidencias para apuntar a `documentos/solicitudes/<uid>/...`, respetando la política `reviewer_id` vigente y evitando el error de RLS al crear solicitudes desde el formulario.
- Añadí un fallback en `SolicitudService.createSolicitud` que detecta el error `PGRST204` (esquema sin columna `tipo`) y reintenta la inserción sin ese campo, prefijando el tipo elegido en la descripción para no perder contexto hasta que la migración llegue al entorno remoto.
- Refiné el formulario de kermesses en `lib/ui/solicitudes/create_solicitud_page.dart`: ahora solicita fecha/hora mediante selector nativo, valida coordenadas GPS, documenta menú, shows y aliados y borra el horario almacenado al cambiar de tipo. Incluí una utilidad `_pickKermesseDateTime` para formatear la fecha con `MaterialLocalizations`.
- Añadí la primera iteración del flujo de revisión administrativa: desde `AdminDashboardPage` se puede abrir cada solicitud pendiente, aprobarla o pedir cambios con un mensaje. `AdminDashboardController` delega en `AdminService.reviewCampaignRequest`, que actualiza `public.solicitudes` e inserta un registro en `public.solicitud_reviews` para transparencia.
- Rediseñé la hoja de revisión de campañas en `lib/pages/admin/admin_dashboard_page.dart` limitando su ancho, agrupando la información en tarjetas y añadiendo indicadores compactos para el ID y la fecha de envío, mejorando la legibilidad cuando se revisa una solicitud.
- Reorganicé el `AdminDashboardPage` completo en `lib/pages/admin/admin_dashboard_page.dart` con un layout responsive de estilo panel: métricas en tarjetas con `Wrap`, bandejas de pendientes distribuidas en dos columnas cuando hay espacio y tiles compactos con información jerarquizada para cada solicitud.
- Añadí un encabezado con botón de retroceso, barra de highlights y encabezado de sección en `lib/pages/admin/admin_dashboard_page.dart` para emular paneles profesionales: el contenido queda centrado con ancho máximo, se muestran atajos con cifras clave y las bandejas pendientes resaltan el estado global.
- Extendí el panel admin para listar campañas aprobadas: `AdminDashboardController`/`AdminService` cargan `AdminActiveCampaign`, `AdminDashboardPage` muestra tarjetas con progreso y estado y el modelo quedó registrado en `lib/models/admin_dashboard.dart`.

## 2025-10-22
- Ajusté las políticas RLS `campanias_creator_insert` y `campanias_creator_update` en `supabase/supabase.sql` para permitir que los administradores inserten o reactiven campañas directamente, garantizando que las aprobaciones desde el panel generen registros en `public.campanias` aunque el creador original sea otro usuario.
- Ajusté `AdminService.reviewCampaignRequest` para que al aprobar una solicitud genere (o reactive) automáticamente la campaña asociada en `public.campanias`, validando el monto objetivo y reutilizando la descripción del formulario. Con esto las campañas aprobadas pasan a la vista pública y aparecen en el home sin pasos manuales.
- Reconstruí `lib/pages/admin/admin_dashboard_page.dart` tras la reestructuración del panel: ahora la navegación separa Solicitudes, Donaciones y Organizaciones con listas completas y hojas de revisión que permiten aprobar o pedir cambios con mensajes.
- Activé políticas RLS específicas para `public.campanias` en `supabase/supabase.sql`: lectura pública para campañas activas, visibilidad total para los creadores y permisos completos para administradores. Esto libera el acceso al `view` `v_campania_publica` y permite que la publicación automática funcione bajo seguridad.
- Añadí políticas de lectura/administración para `public.categorias` y creé las funciones `public.list_public_campaigns` y `public.search_public_campaigns` (ambas `SECURITY DEFINER`) para exponer campañas activas mediante RPC, con agregados de donadores y metadatos listos para la app. `CampaignService` ahora consume estas funciones, asegurando resultados aunque RLS limite consultas directas a las vistas.
- Incorporé la función `public.publish_campaign_from_solicitud` en `supabase/supabase.sql`, ejecutada como `SECURITY DEFINER`, que valida permisos admin, publica o reactiva la campaña y registra la revisión en una sola transacción.

## 2025-10-30
- Implementé algoritmo inteligente de recomendación en `campaign_tab_view.dart` que combina múltiples factores: actividad reciente (0-90 días), progreso de financiamiento (sweet spot 20-80%), engagement de donadores (5-50+), magnitud del proyecto (Bs 5k-200k) y favoritos del usuario. El sistema asigna puntuaciones ponderadas para ordenar campañas por relevancia.
- Rediseñé `SortToggleBar` con chips profesionales que incluyen iconos, fondo blanco con sombra sutil y estado seleccionado con color azul primario. Los filtros ahora son más visuales y funcionales: Recomendado (algoritmo inteligente), Más recientes (por fecha), Mayor avance (% de meta) y Más donadores (cantidad).
- Mejoré la experiencia visual de los filtros con altura fija de 48px, scroll horizontal suave, padding interno consistente y transiciones claras entre estados seleccionado/no seleccionado usando Material InkWell para el feedback táctil.
- Actualicé `AdminService.reviewCampaignRequest` para delegar la aprobación en la nueva RPC y manejar las observaciones como rechazo simple, evitando los errores silenciosos que impedían crear registros en `public.campanias`.
- Eliminé el `CampaignHero` de `lib/ui/home/home_page.dart` para que la sección inicial de campañas sea más compacta y el contenido relevante aparezca de inmediato en la lista principal.
- Dividí `lib/ui/solicitudes/create_solicitud_page.dart` en componentes reutilizables (`lib/ui/solicitudes/steps/solicitud_type_step.dart`, `lib/ui/solicitudes/steps/solicitud_profile_review_step.dart`, `lib/ui/solicitudes/steps/solicitud_form_step.dart`) para organizar el flujo por pasos y reducir el tamaño del archivo principal.
- `CreateSolicitudPage` ahora delega la selección de tipo, la revisión de perfil y el formulario final en widgets separados, simplificando la lectura del código y permitiendo mantener la lógica de validaciones y cargas de evidencias sin duplicación.
- Reorganicé el flujo de kermesses: el formulario permite escoger la ubicación desde un mapa de Google (`KermesseLocationPickerPage`), capturar menú y actividades como listas dinámicas y compartir una galería opcional de imágenes además de la portada.
- `SolicitudFormStep` gestiona los nuevos modelos (`SolicitudKermesseLocation`, `SolicitudKermesseMenuItem`, `SolicitudKermesseActivity`) y expone callbacks para añadir/editar entradas sin duplicar lógica en la vista principal.
- Añadí la dependencia `google_maps_flutter` en `pubspec.yaml` y ejecuté `flutter pub get`; falta registrar la API key de Maps en Android/iOS para habilitar la vista.

## 2025-10-23
- Sustituí la vista de Google Maps por `flutter_map` con mosaicos de OpenStreetMap en `KermesseLocationPickerPage`, eliminando la necesidad de API keys y resolviendo el lienzo gris que se mostraba al abrir el selector.
- Ajusté el botón "Centrar en mi ubicación" para que marque automáticamente el punto y obtenga la dirección de referencia.
- Reemplacé `google_maps_flutter` por `flutter_map`/`latlong2` en `pubspec.yaml` y actualicé dependencias con `flutter pub get`.
- `AdminDashboardPage` ahora muestra un distintivo según el tipo de solicitud (resaltando las kermesses) para evitar confusiones en la bandeja de pendientes.
- `AdminService.reviewCampaignRequest` detecta solicitudes no campanarias y las aprueba directamente sin invocar la RPC de publicación, permitiendo autorizar kermesses desde el panel.
- Renové la tarjeta de "Confirma tus datos" en `SolicitudProfileReviewStep`: avatar inicial, pills de contacto y tamaños de fuente equilibrados.
- Los datos sensibles (cuenta bancaria y enlace QR) ahora se muestran ocultos por defecto con botón de visibilidad y mensajes de contexto.
- La tarjeta de revisión de perfil ahora muestra el QR de donaciones como imagen cuando se habilita la visibilidad, evitando exponer la URL sin dejar de protegerla por defecto.
- Ajusté el encabezado de `AdminDashboardPage` para que el texto se contraiga con elipsis en pantallas angostas, eliminando el aviso de overflow que mostraba la franja amarilla/negra en modo debug.

## 2025-10-24
- Simplifiqué `HomePage` retirando `CampaignHero` y los filtros por categoría para que el feed arranque directo con la lista de secciones y evitar la banda visual que aparecía debajo del hero.
- Añadí `_SortToggleBar` en `lib/ui/home/home_page.dart`, un grupo de `ChoiceChip` que permite alternar entre "Recomendado", "Más recientes", "Mayor avance" y "Más donadores" sin menús adicionales.
- Protegí el flujo de búsqueda en `HomePage` verificando `mounted` antes de abrir detalles tras cerrar el `showSearch`, evitando el error "widget has been unmounted" cuando el usuario cambia de pantalla durante la búsqueda.
- Ajusté la sección "Recién lanzadas" para mostrar solo las tres campañas más nuevas, evitando el listado largo que desplazaba el resto del feed.
- Habilité favoritos desde la vista principal: `CampaignSummary` ahora expone `isFavorite`, `CampaignService` marca y persiste el estado usando el endpoint `favoritos`, `CampaignController.toggleFavorite` gestiona el optimismo y las tarjetas (`CampaignCard`, `_CampaignProgressTile`, `_CampaignHeadlineTile`) muestran un botón de corazón.
- Añadí políticas RLS `favoritos_select_self`, `favoritos_insert_self` y `favoritos_delete_self` en `supabase/supabase.sql` para que cada usuario pueda administrar sus propias campañas favoritas.
- Implementé favoritos reales en `HomePage`: `CampaignCard`, `_CampaignProgressTile` y `_CampaignHeadlineTile` ahora muestran un botón de corazón que llama a `CampaignController.toggleFavorite`, mientras `CampaignService` lee/escribe la tabla `favoritos` para persistir el estado.
- Renové `lib/ui/home/campaign_detail_page.dart` para mostrar historia extendida, ubicación, equipo organizador y acceso directo al video, habilité la visualización de evidencias (con visor para fotos y apertura de enlaces) y añadí una hoja modal con formulario para registrar donaciones reales.
- Incorporé `CampaignService.createDonation` y nuevas políticas RLS `donaciones_public_read`, `donaciones_insert_self` y `donaciones_admin_manage` en `supabase/supabase.sql`, permitiendo que cualquier usuario autenticado registre aportes mientras los administradores mantienen el control de validación.
- Creé `lib/ui/home/favorites_page.dart`, una vista dedicada que lista las campañas favoritas con estado reactivo del `CampaignController`, vacíos amigables y acceso directo a detalles y apoyo.
- Conecté el drawer de `HomePage` al nuevo `FavoriteCampaignsPage` mediante `_openFavorites`, reutilizando los callbacks existentes para abrir campañas, apoyar y alternar el corazón dentro de la lista especializada.
- Enriquecí la hoja de donaciones en `lib/ui/home/campaign_detail_page.dart`: ahora muestra instrucciones dinámicas por método (QR, transferencia u otros), permite abrir el QR en grande, copiar enlaces/datos y ofrece mensajes cuando faltan configuraciones.
- Permití adjuntar la imagen del comprobante desde la galería con vista previa, validación de peso y opciones para reemplazarla o eliminarla antes de enviar la donación.
- `CampaignService.createDonation` recupera el perfil del creador para derivar instrucciones de pago, recibe los bytes del comprobante, los sube al bucket `comprobantes` y adjunta la URL resultante al registro.
- `CampaignDetail` suma `CampaignPaymentInstructions` y el teléfono del organizador para reutilizar los datos bancarios/QR en la UI sin duplicar llamadas.
- Creé la función `public.get_campaign_payment_instructions` (SECURITY DEFINER) en `supabase/supabase.sql` y actualicé `CampaignService.fetchCampaignDetail` para consumirla, exponiendo datos bancarios y QR a los donantes sin relajar las políticas RLS de `profiles`.
- Fortalecí `CampaignService.fetchCampaignDetail` para que los errores de RLS en recompensas, actualizaciones o evidencias no bloqueen la respuesta principal, asegurando que las instrucciones de pago lleguen al formulario de donaciones incluso cuando esos listados no son visibles.
- Habilité a los roles `anon` y `authenticated` para ejecutar `public.get_campaign_payment_instructions`, de modo que los datos bancarios y el QR se muestren al seleccionar el método de pago aunque el visitante aún no haya iniciado sesión.
- Simplifiqué las acciones del QR en la hoja de donaciones (`lib/ui/home/campaign_detail_page.dart`) dejando solo el botón "Descargar".
- Corregí `CampaignService.createDonation` para preservar los mensajes de error propios y ajusté la carga de comprobantes al path `users/<uid>/donaciones/<campania>` que cumple con la política del bucket `comprobantes`, evitando fallos silenciosos al registrar donaciones.

## 2025-10-25
- Hice obligatorio adjuntar el comprobante en la hoja de donaciones (`lib/ui/home/campaign_detail_page.dart`), actualizando los textos guía y el validador del formulario para impedir el envío si falta la imagen y clarificando que la evidencia es necesaria para registrar la donación.
- Eliminé el campo de referencia textual en la hoja de donaciones y reorganicé el formulario (`lib/ui/home/campaign_detail_page.dart`) con secciones más claras, validación explícita del comprobante y mensajes contextuales que mejoran la experiencia de usuario al registrar aportes.
- Reemplacé la implementación monolítica de `lib/pages/admin/admin_dashboard_page.dart` para consumir las secciones modulares `lib/pages/admin/sections/admin_section_widgets.dart`, `campaign_requests_section.dart`, `donations_section.dart` y `organizations_section.dart`, limpiando widgets duplicados y dejando el archivo principal enfocado en navegación, métricas y orquestación del panel.
- Añadí `AdminMetricsPanel` en `lib/pages/admin/sections/metrics_section.dart` para mostrar un resumen ejecutivo con totales combinados, montos aprobados, metas activas y el avance promedio por campaña, sustituyendo los contadores básicos y reforzando la lectura del panel.
- Incorporé la pestaña “Métricas” en la navegación inferior/derecha del panel (`AdminDashboardSection.metrics`), mostrando la vista anterior como sección dedicada y sumando el total de pendientes como badge del ícono.
- Reemplacé `lib/pages/admin/sections/donations_section.dart` con una sección sólida que abre `showDonationDetailSheet`, mostrando la hoja modal con el `AdminDonationDetail` completo que pinta montos, estado, comprobante y metadatos sin depender del placeholder anterior.
- Habilité la revisión completa de donaciones: la hoja modal ahora incluye botones para aceptar o rechazar, maneja errores inline y muestra progreso mientras se ejecuta la acción.
- Añadí `AdminService.reviewDonation` junto con `AdminDashboardController.approveDonation/rejectDonation`; al aprobar se actualiza la tabla `donaciones`, se registra el validador y se recarga el panel para que las campañas activas reflejen de inmediato el nuevo monto acumulado.
- Ajusté las políticas RLS de `public.notificaciones` en `supabase/supabase.sql` agregando `notificaciones_insert_flow`, permitiendo que triggers y administradores inserten avisos sin chocar con RLS cuando se aprueban donaciones o se registran solicitudes.
- Creé `KermesseService`/`KermesseController` junto al modelo `KermesseSummary` para consumir las solicitudes de tipo kermesse aprobadas con políticas dedicadas, dejando lista la capa de datos para los listados públicos.
- Agregué `KermesseListPage` (`lib/ui/kermesses/kermesse_list_page.dart`) y enlacé el drawer del home para abrirla, mostrando tarjetas con fecha, lugar, menú y actividades además de un detalle completo del evento.
- Extendí `supabase/supabase.sql` con la política `solicitudes_public_kermesse_read`, habilitando que usuarios autenticados (o visitantes) consulten las kermesses aprobadas sin romper las restricciones existentes.
- Ajusté `HomePage` para inicializar el controlador de kermesses antes del primer acceso, solucionando el `LateInitializationError` al abrir el nuevo listado.
- Renové `KermesseDetailPage` con encabezado tipo hero, chips de metadatos y tarjetas con secciones (ubicación, impacto, menú, actividades), ofreciendo una presentación más clara y consistente con el resto de la app.
- Eliminé la expansión de descripción completa e imágenes en `KermesseDetailPage`, incorporando un encabezado degradado, tarjetas sin portadas y un mapa interactivo con `flutter_map` cuando hay coordenadas, dejando la sección de ubicación lista para futuras mejoras.
- Actualicé el parser de `KermesseSummary` para detectar coordenadas provenientes de la descripción en formato libre y mostré el mapa al inicio de la sección de ubicación, garantizando que las kermesses con latitud/longitud cargadas desde el formulario se rendericen correctamente sin depender de campos duplicados.
- Extendí `KermesseSummary` para extraer las URLs de la sección “Evidencias fotográficas” y mostré una galería horizontal en `KermesseDetailPage`, de modo que las imágenes subidas al registrar la kermesse queden visibles para los asistentes.
- Añadí un botón “Abrir en mapa” y un fallback que abre Google Maps o la app nativa desde `KermesseDetailPage`, reutilizando las coordenadas normalizadas para que los asistentes ubiquen rápidamente el evento.
- Creé `DonationHistoryPage` y su flujo asociado (`DonationHistoryController`/`DonationHistoryService`/`DonationHistoryEntry`) para que desde el menú lateral los usuarios consulten sus aportes, estados de revisión y comprobantes descargables.
- Ajusté el historial para mostrar los comprobantes en un visor dentro de la app, evitando abrir otras aplicaciones y permitiendo hacer zoom sobre la imagen adjunta.

## 2025-10-26
- Modelé las organizaciones aprobadas en `lib/models/organization.dart`, encapsulando estado, contactos y metadatos para reutilizarlos en la UI.
- Incorporé `OrganizationService` (`lib/services/organization_service.dart`) y `OrganizationController` (`lib/controllers/organization_controller.dart`) para consultar Supabase, segmentar destacadas/recientes y exponer un estado reactivo.
- Añadí componentes visuales reutilizables en `lib/ui/home/widgets/organization_card.dart` para presentar organizaciones en tarjetas destacadas y filas compactas.
- Reescribí `lib/ui/home/home_page.dart` para soportar una pestaña de organizaciones con secciones dinámicas, recarga independiente y hoja modal de detalles con acciones de contacto.
- Reconstruí `lib/ui/home/home_page.dart` conectándolo a `menu_inferior/campaign_tab_view.dart` y `menu_inferior/organization_tab_view.dart`, restaurando navegación, búsqueda, drawer y acciones de contacto tras modularizar las pestañas de Campañas y Organizaciones.

## 2025-10-27
- Creé `CreateOrganizationPage` (`lib/pages/organizations/create_organization_page.dart`), un formulario completo que permite registrar perfiles provisionales de organización, subir un logo opcional y confirmar la veracidad de la información antes de enviarla a revisión administrativa.
- Implementé `OrganizationDraft` en `lib/models/organization.dart`, `OrganizationRegistrationController` en `lib/controllers/organization_registration_controller.dart` y extendí `OrganizationService` para soportar la subida de logos y el alta de organizaciones con estado `pendiente`.
- Actualicé `OrganizationTabView` y `lib/ui/home/home_page.dart` para exponer el CTA "Crear organización" dentro de la pestaña, reaprovechando la validación de perfil y mostrando mensajes de confirmación tras enviar la solicitud.
- Amplié el registro de organizaciones para incluir galería de fotos del lugar y múltiples enlaces de redes sociales, subiendo evidencias a Supabase Storage y anexándolas en la descripción enviada al panel admin.
- Añadí políticas RLS en `supabase/supabase.sql` para `public.organizaciones`, permitiendo lecturas públicas de las verificadas, acceso del propietario a sus borradores, inserciones autenticadas y control total para administradores sin romper la revisión manual.
- Reconstruí `lib/pages/admin/sections/organizations_section.dart` con tarjetas de revisión y una hoja modal que carga `AdminOrganizationDetail`, permite abrir documentos y aprobar o rechazar organizaciones con notas directas al panel.
- Homologué la pestaña principal del home: `CampaignTabView` y `OrganizationTabView` usan el nuevo `HomeSection`, el FAB del `HomePage` alterna entre crear campaña o registrar organización y desaparecieron los botones flotantes duplicados, dejando un layout consistente en ambas pestañas.
- Simplifiqué la barra superior de `HomePage` eliminando el acceso directo al perfil (ya disponible en el menú lateral) para reducir ruido visual en la cabecera.
- Implementé un centro de notificaciones interno: `NotificationController`/`NotificationService` consultan Supabase, `NotificationsPage` lista avisos con detalle y permite marcarlos como leídos individualmente o en bloque, mientras el icono del encabezado y el drawer muestran el contador en vivo.
- Conecté el controlador de notificaciones al canal Realtime de Supabase para insertar/actualizar eliminaciones al instante sin recargar manualmente la lista.
- Ajusté `_buildNotificationsAction` en `lib/ui/home/home_page.dart` envolviendo el botón en un `AnimatedBuilder` para que el icono y el badge reflejen de inmediato los cambios al cargar o actualizar las notificaciones.
- Amplié el trigger `public.on_donacion_aprobada` en `supabase/supabase.sql` para notificar tanto al creador como al donante cuando se aprueba una donación y avisar al donante cuando se rechaza, con mensajes que incluyen el título de la campaña cuando está disponible.
- Añadí `public.get_user_notifications` (Security Definer) y actualicé `NotificationService.fetchNotifications` para consumirla, garantizando que cada usuario cargue solo sus avisos aun con RLS activo.
- Incorporé triggers `on_solicitud_estado_change` y `on_organizacion_estado_change` en `supabase/supabase.sql` para que solicitantes y dueños de organizaciones reciban notificaciones automáticas al aprobar o rechazar sus procesos.
- Rediseñé `lib/ui/home/notifications/notifications_page.dart`: nuevo banner contextual, agrupación por fecha y tarjetas con chips, gradientes e información de payload para adaptar la experiencia a apps modernas de notificaciones.
- Modelé las recompensas reclamadas en `lib/models/user_reward_entry.dart`, reutilizando el estado de las donaciones para exponer montos, disponibilidad y fechas de validación por beneficio.
- Creé `UserRewardService` y `UserRewardsController` para consultar desde Supabase las recompensas del usuario autenticado, con manejo de errores y recarga manual.
- Añadí `UserRewardsPage` (`lib/ui/rewards/user_rewards_page.dart`) y la opción "Recompensas" en el drawer de `HomePage`, habilitando un resumen dedicado con tarjetas que muestran estado (pendiente, disponible o rechazado) y metadatos clave.
- Ajusté el flujo de donaciones para asignar automáticamente la recompensa más alta disponible cuando el usuario no selecciona una manualmente, reutilizando la lógica tanto en `_DonationSheet` como en `CampaignService.createDonation` y expandiendo `CampaignReward` para conocer límites/reclamaciones.

## 2025-10-29
- Refiné `lib/ui/home/campaign_detail_page.dart` para detectar enlaces de imagen dentro de la historia y mostrarlos embebidos en lugar de enlaces planos, mejorando la narrativa visual de las campañas.
- Rediseñé la sección de evidencias en `lib/ui/home/campaign_detail_page.dart` con tarjetas responsivas que priorizan las fotografías, agregan overlays informativos y mantienen acceso rápido al visor de detalles.
- Registré los cambios anteriores en este `dev_log` para documentar la mejora de experiencia en la vista de detalle de campañas.
- Normalicé el parseo del campo `leido` dentro de `NotificationEntry` para respetar los booleanos devueltos por Supabase (true/false, t/f, 1/0) y evitar que al recargar la lista las notificaciones vuelvan a aparecer como no leídas.
- Creé las funciones `mark_notification_as_read` y `mark_notifications_as_read` (security definer) en `supabase/supabase.sql` y actualicé `NotificationService` para usarlas, garantizando que el cambio de estado se persista en la base aun con RLS activo.
- Expandí `UserRewardService`/`UserRewardEntry` para incluir todas las donaciones del usuario, aun cuando no exista `recompensa_id`, y la UI de `UserRewardsPage` ahora indica cuándo falta asignar recompensa y por qué podría ocurrir.
- Mejoré `public.on_donacion_aprobada`: si la donación aprobada no tiene `recompensa_id`, la función selecciona automáticamente la recompensa más alta disponible según el monto aportado, actualiza `donaciones.recompensa_id` y aumenta `cantidad_reclamada`, garantizando que las recompensas se asignen sin intervención manual.
- Sustituí el sistema de recompensas por un ranking global de trofeos: añadí las funciones `get_donor_leaderboard`, `get_current_user_trophy_profile` y el helper `resolve_trophy_level` en `supabase/supabase.sql`, junto con la política de ejecución para `anon`/`authenticated`.
- Creé `DonorTrophyService`, `DonorTrophyController`, los modelos `DonorTrophyEntry/DonorTrophyProfile` y la vista `DonorTrophiesPage`, reemplazando la antigua pantalla de recompensas con un ranking de top 3, tabla completa y resumen del progreso personal.
- Integré el ranking en el home: `CampaignTabView` muestra una tarjeta con el Top 3 y acceso directo al ranking completo, `HomePage` inicializa el nuevo controlador y el drawer abre `DonorTrophiesPage`.
- Dejé `user_reward_*` como exports de compatibilidad para facilitar la migración de imports existentes hacia los nuevos nombres.
- Actualicé `get_donor_leaderboard` y `get_current_user_trophy_profile` en `supabase/supabase.sql` envolviendo el campo `position` con comillas para evitar el conflicto con la palabra reservada y permitir que la migración se aplique correctamente.
- Ajusté ambas funciones de trofeos para castear el `row_number()` a entero antes de llamar a `resolve_trophy_level`, evitando el error de tipos al ejecutar la migración en Postgres.
- Renombré la columna temporal `position` a `ranking_position` en las funciones del ranking para prevenir la ambigüedad al ordenar y exponer el campo `"position"` en el resultado.
- Ajusté el formulario de solicitudes: el título ahora acepta cuatro palabras como máximo, muestra ejemplos concretos en la ayuda inferior sin recordatorios numéricos y se recorta al enviar, mientras que la historia se limita a 300 caracteres.
- Expandí el selector de relación "Soy beneficiario" en `SolicitudFormStep` para evitar overflows en pantallas angostas.
- Extendí `CampaignController` para separar campañas completadas del resto, recalculando favoritos y segmentos a partir de esa división para que el home solo promueva campañas activas.
- Actualicé `CampaignTabView`, `CampaignCard` y `CampaignProgressTile` para truncar títulos a dos líneas, añadir la insignia de meta alcanzada y mostrar una sección dedicada a campañas completadas sin botón de apoyo.
- Ajusté la altura del carrusel de campañas destacadas en `CampaignTabView` para evitar el overflow tras añadir la insignia de meta alcanzada dentro de las tarjetas.
- En `CampaignDetailPage` oculté la barra de apoyo y el CTA del resumen cuando la campaña está completada, de modo que no se pueda iniciar una donación adicional desde el detalle.
- Pulí `SolicitudFormStep` añadiendo contador de palabras, validaciones más claras y ejemplos con acentos correctos; actualicé los textos guía de tipo de solicitud y las opciones de relación con el beneficiario para un tono más profesional.
- `CampaignService.fetchCampaignDetail` incorpora los agregados de `v_campania_publica` (monto actual, meta, porcentaje y donadores) para que el detalle reutilice exactamente la misma métrica que las tarjetas públicas sin duplicar lógica.
- Rediseñé la sección de historia en `CampaignDetailPage` con jerarquía visual, encabezados resaltados, bullets y tarjetas para enlaces, facilitando la lectura en móviles y evitando bloques de texto planos.
- Reconstruí `CampaignDetailPage` después de una corrupción del archivo: restauré el widget principal, el visor de evidencias y el flujo de donaciones, añadí un handler de enlaces externos con feedback y un atajo para compartir la campaña copiando el enlace público.
- Extraí la lógica de historia en un `_StoryBody` modular que interpreta imágenes Markdown y URLs incrustadas, genera `RichText` con spans clicables y presenta un `LinkTile` consistente cuando no se trata de contenido multimedia.
- Extendi `_StoryBody` para renderizar URLs de imagen embebidas dentro de líneas de texto (no solo cuando están solas), separando el texto descriptivo del bloque multimedia y mostrando cada foto como preview táctil en lugar de un enlace resaltado.
- Evolucioné `_StoryBody` para agrupar imágenes consecutivas en un carrusel con visor modal interno, habilitando navegación con indicadores y zoom sin depender de abrir el navegador externo.
- Ajusté el carrusel de historia para mostrarse como galería horizontal con miniaturas alineadas, manteniendo el visor modal para ampliar/zoomear sin salir del detalle de campaña.
- Renové la sección de evidencias en `CampaignDetailPage` con un carrusel horizontal de tarjetas para que las pruebas visuales queden una al lado de la otra, más un visor modal de fondo claro y progresivo en lugar del overlay negro.
- Reorganicé `CampaignDetailPage` en la carpeta `lib/ui/home/menu_inferior/campaign_detail/`, separando la vista principal, la historia, organizador, evidencias, comentarios y hoja de donaciones en archivos parciales, además de actualizar `HomePage` y remover la versión monolítica anterior para evitar duplicidades.
- Profesionalicé la sección de evidencias en `lib/ui/home/menu_inferior/campaign_detail/evidence_section.dart`: incorpora resumen validado, filtros por tipo y una cuadrícula responsiva de tarjetas con overlay limpio, detección de descripciones placeholder y CTA ligero "Ver evidencia".
- Refiné `_normalizeEvidenceDescription` en `lib/ui/home/menu_inferior/campaign_detail/evidence_section.dart` para limpiar viñetas Unicode, elegir la primera línea significativa y eliminar los bullets residuales que se mostraban sobre las miniaturas.
- Ajusté nuevamente `_normalizeEvidenceDescription` en `lib/ui/home/menu_inferior/campaign_detail/evidence_section.dart` para ignorar separadores Unicode adicionales y descartar cualquier descripción que aún contenga viñetas, garantizando que no reaparezcan puntos sobre las tarjetas.
- Reconstruí `lib/ui/home/menu_inferior/campaign_detail/story_section.dart` con un parser nuevo que agrupa encabezados, párrafos, listas e imágenes sin depender de los caracteres de viñeta originales; ahora los bullets usan indicadores personalizados y las galerías se combinan en carruseles limpios sin mostrar puntos inesperados.
- Creé un visor de evidencias a pantalla completa (`_EvidenceViewerPage`) que reemplaza el diálogo básico: mantiene la animación Hero, permite hacer zoom con `InteractiveViewer`, muestra la descripción saneada y ofrece accesos claros para abrir el archivo original o cerrar el visor.
- Actualicé `_EvidenceViewerPage` con un fondo claro difuminado, tarjetas suaves y botones flotantes blancos para alinear el visor de evidencias con el estilo profesional solicitado y evitar el overlay negro previo.
- Habilité comentarios funcionales en `CampaignDetailPage`: el servicio ahora inserta y lee `comentarios` (capturando snapshot de nombre/avatar), el modelo `CampaignComment` interpreta `contenido/estado`, la vista muestra un composer con validaciones/feedback y se refresca la lista tras publicar.
- Definí políticas RLS `comentarios_select_visible` y `comentarios_insert_self` en `supabase/supabase.sql` para permitir leer comentarios visibles (anon/autenticados) y publicar nuevos mensajes bajo sesión autenticada.
- Extendí `supabase/supabase.sql` para añadir columnas `autor_nombre` y `autor_avatar_url` en `public.comentarios`, almacenando los datos visibles del autor al momento de comentar y evitando depender de joins restringidos.
- Reemplacé `lib/ui/home/menu_inferior/campaign_detail/comments_section.dart` por una sección minimalista inspirada en apps sociales: contenedor con sombras sutiles, header con contador animado, composer multilinea y listado scrollable; todo usa exclusivamente `Theme.of(context).colorScheme` para seguir la paleta institucional.
- Compacté la nueva sección de comentarios equilibrando tipografías, paddings y contenedores para que el header, el compositor y las tarjetas de mensajes ocupen menos espacio y mantengan proporciones consistentes en producción.
- Reconstruí la hoja de compartir reutilizando `share_sheet.dart`, con encabezado y opciones de redes alineadas a la paleta para copiar el enlace o abrir canales externos desde el detalle de campaña.
- Actualicé la pestaña de campañas del home para mostrar montos en bolivianos (`Bs`) tanto en las tarjetas como en los listados compactos.
- Refiné el encabezado del drawer del home: añadí `SafeArea`, aumenté el padding, ajusté tipografías y mantuve el gradiente institucional para el logo y el tagline.
- Mejoré la sección de historia de campañas (`story_section.dart`): encabezados full-width con borde, listas agrupadas con fondo y separación mejorada (12px), viñetas circulares con borde (8px), enlaces más visibles con fondo y borde, carrusel de imágenes más espacioso (16px separación, 200px alto), visor modal con botón de cerrar destacado y contador con sombra.
- Ajusté la sección de comentarios para usar el mismo ancho y estructura que otras secciones (usando `_SectionCard`), eliminando padding redundante y usando un header más compacto.
- Implementé detección automática y resaltado de etiquetas importantes en la historia (Nombre:, Edad:, Necesidad:, etc.) con negritas y color azul primario para mejor legibilidad de datos del beneficiario.
- **Cover Header mejorado**: Agregué gradiente oscuro inferior para mejor legibilidad del logo, botón flotante de compartir (top-left), badge de categoría más grande (18px icon, mejor padding).
- **Estadísticas responsive**: Implementé diseño de 3 columnas para pantallas medianas (380-520px), barra de progreso con porcentaje embebido y label dinámico, bloques "Recaudado" y "Meta" destacados con iconos más grandes (22px), color verde y borde más grueso.
- **Recompensas con jerarquía visual**: Añadí icono de regalo junto al título, precio destacado en badge con gradiente y sombra, badge "LIMITADA" naranja para recompensas con ≤5 unidades, borde naranja en tiles de stock bajo, iconos para entrega y disponibilidad.
- **Comentarios optimizados**: Reducción profesional de tamaños - texto 13px (antes bodyMedium), avatares 18px radius (antes 20px), padding 14px (antes 16px), bordes redondeados 12px (antes 14px), altura lista 300px (antes 320px), separación 12px (antes 16px), campo de texto 2-4 líneas (antes 3-5), botones e iconos compactados, hint text más motivador.
- **Share Sheet rediseñado estilo premium**: Grid 2x2 con tarjetas limpias tipo iOS/Material 3, iconos circulares sólidos 64px con colores oficiales (WhatsApp #25D366, Facebook #1877F2, Gmail #EA4335), sombras de color intensas, padding 18px uniforme, labels cortos sin cortes ("WhatsApp", "Facebook", "Correo", "Copiar enlace"), descripciones ultra-cortas ("Tus contactos", "Tu muro", "Email", "Portapapeles"), textos con overflow visible para evitar cortes, iconos 32px blancos, label 15px bold letter-spacing -0.3, descripción 12px peso 500, SIN franjas amarillas/negras, diseño limpio moderno profesional.
